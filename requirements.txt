# Direct dependencies of pipeline/ and scripts/, pinned to versions verified
# working in this project's own .venv (via `pip freeze`, see
# requirements_freeze_raw.txt for the full transitive closure of every
# installed package if you need an exact-reproduction environment instead
# of just the direct dependencies below).
#
# Regenerated 2026-07-28: the previous version of this file (langchain==
# 0.0.123, chromadb==0.3.26, llama-cpp-python, ctransformers) reflected an
# earlier, now-orphaned prototype (main.py/agent.py/chat_interface.py) that
# is no longer imported by anything the paper evaluates -- see that
# section's own comment for why those files are kept but not deleted.
# Generation now goes through a locally hosted Ollama server via plain HTTP
# (pipeline/ollama_client.py uses `requests`, not the `ollama` PyPI package
# or langchain), so torch/cuda is needed only for embedding fine-tuning,
# reranking, and the sentence-embedding/cross-encoder models themselves --
# not for the LLM used for generation.

# Retrieval
chromadb==1.5.9
rank-bm25==0.2.2
networkx==3.6.1

# Embeddings / reranking / fine-tuning
sentence-transformers==5.6.1
# NOTE: the +cu121 local-version tag is not on PyPI -- installing this pin
# requires PyTorch's own wheel index, e.g.:
#   pip install torch==2.5.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121
# (or drop the +cu121 suffix for a CPU-only environment; everything in
# pipeline/ works on CPU, just slower).
torch==2.5.1+cu121
transformers==5.14.1
accelerate==1.14.0
datasets==5.0.0
scikit-learn==1.9.0

# Evaluation metrics
rouge_score==0.1.2
bert-score==0.3.13
nltk==3.10.0

# Generation client (Ollama over HTTP, local server -- see pipeline/ollama_client.py)
requests==2.34.2

# Data / stats / plotting
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
matplotlib==3.11.1

huggingface_hub==1.24.0
